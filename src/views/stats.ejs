
<div class="header">
  <h1 class="title">Link Analytics</h1>
  <p class="text-muted">Detailed statistics for your short link</p>
</div>

<div class="stats-card">
  <div class="stats-header">
    <div>
      <h2 class="text-primary"><%= link.code %></h2>
      <p class="text-muted">Short Code</p>
    </div>
    <div class="stats-actions">
      <a 
        href="/<%= link.code %>" 
        target="_blank" 
        class="btn btn-primary"
      >
        <i class="fas fa-external-link-alt"></i> Open Link
      </a>
      <a 
        href="/" 
        class="btn btn-outline-secondary"
      >
        <i class="fas fa-arrow-left"></i> Back to Dashboard
      </a>
    </div>
  </div>

  <div class="stats-grid">
    <div class="stat-item">
      <div class="stat-value"><%= link.clicks %></div>
      <div class="stat-label">Total Clicks</div>
    </div>
    <div class="stat-item">
      <div class="stat-value">
        <%= link.lastClicked 
          ? new Date(link.lastClicked).toLocaleDateString() 
          : 'Never' 
        %>
      </div>
      <div class="stat-label">Last Clicked</div>
    </div>
    <div class="stat-item">
      <div class="stat-value">
        <%= new Date(link.createdAt).toLocaleDateString() %>
      </div>
      <div class="stat-label">Created On</div>
    </div>
  </div>

  <div class="mt-4">
    <h3>Destination URL</h3>
    <div class="url-display">
      <span class="url-text"><%= link.url %></span>
      <button 
        class="btn btn-sm btn-info"
        onclick="copyText('<%= link.url %>')"
        title="Copy URL"
      >
        <i class="far fa-copy"></i> Copy
      </button>
    </div>
  </div>

  <% if (link.lastClicked) { %>
    <div class="mt-4">
      <h3>Last Clicked</h3>
      <p><%= new Date(link.lastClicked).toLocaleString() %></p>
    </div>
  <% } %>
</div>

<style>
  .stats-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .stats-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .btn-outline-secondary {
    background-color: transparent;
    border: 1px solid #dee2e6;
    color: #6c757d;
  }

  .btn-outline-secondary:hover {
    background-color: #f8f9fa;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .stat-item {
    background: #f8f9fa;
    border-radius: var(--border-radius);
    padding: 1.5rem;
    text-align: center;
    transition: transform 0.2s ease;
  }

  .stat-item:hover {
    transform: translateY(-2px);
  }

  .stat-value {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary-color);
    margin-bottom: 0.5rem;
  }

  .stat-label {
    color: #6c757d;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .url-display {
    display: flex;
    align-items: center;
    background: #f8f9fa;
    border-radius: var(--border-radius);
    padding: 0.75rem 1rem;
    margin-top: 0.5rem;
  }

  .url-text {
    flex: 1;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    margin-right: 1rem;
  }

  @media (max-width: 768px) {
    .stats-header {
      flex-direction: column;
      align-items: flex-start;
    }

    .stats-actions {
      width: 100%;
      margin-top: 1rem;
    }

    .stats-actions .btn {
      flex: 1;
      text-align: center;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

